# LESSON-9 — Новый React 19

Ветка: lesson-9

## Запуск

npm ci && npm run dev
npm run build

## Чек-лист

Форма управляется через useActionState — 1 балл.
Асинхронная логика реализована корректно — 1 балл.
UI отражает статусы (idle, saving, success) — 1 балл.
Поля формы сбрасываются после успешного сабмита — 1 балл.

Используется useOptimistic — 1 балл.
Задача появляется мгновенно в списке — 2 балла.
Реализация работает без багов и дублирования — 2 балла.

Используется редюсер внутри useActionState — 1 балл.
Все поля формы обрабатываются и отправляются — 1 балл.s
После отправки форма сбрасывается — 1 балл.

## Не сделано / вопросы

- ...
